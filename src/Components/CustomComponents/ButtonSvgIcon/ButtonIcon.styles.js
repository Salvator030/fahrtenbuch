import {StyleSheet} from 'react-native';

const defaultSize = 30;

export const styles = StyleSheet.create({
  root: {
    height: defaultSize,
    width: defaultSize,
    marginTop: 8,
    marginBottom: 16,
  },

  text: {height: 0},
});
