import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  root: {height: 400, width: 300},
  container: {
    flex: 1,
  },
  scrollView: {
    borderColor: 'black',
    borderWidth: 1,
  },
});
