/*
import {TouchableHighlight, View} from 'react-native';

function Button({onPress, size, svg}) {
  return (
    <TouchableHighlight onPress={onPress}>
      <View style={{width: size, height: size}}>{svg}</View>
    </TouchableHighlight>
  );
}
*/